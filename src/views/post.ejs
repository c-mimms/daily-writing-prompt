<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" type="text/css" href="/css/post.css">
  <title>Single Post</title>
</head>

<body>
  <h1 class="title">Single Post</h1>
  <div class="post-details">
      <p><a href="/u/<%= post.author?.id %>"><%= post.author?.username%></a> <%= post.timestamp.toLocaleString('en-US') %>
      </p>
    </p>
    <p><%= post.content %>
    </p>
  </div>

  <div>
    <h2> Similar posts </h2>
    <% for (let i = 0; i < similarPosts.length; i++) { %>
      <div class="post-details">
        <p><a href="/u/<%= similarPosts[i].author?.id %>"><%= similarPosts[i].author?.username%></a> <%= similarPosts[i].timestamp.toLocaleString('en-US') %>
        </p>
        <p><%= similarPosts[i].content %>
        </p>
      </div>
    <% } %>
  </div>
  <hr>
</body>
</html>
