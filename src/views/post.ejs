<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" type="text/css" href="/css/home.css">
  <title>Single Post</title>
</head>

<body>
  <h1 class="title">Single Post</h1>
  <div class="post-details">
      <p><a href="/u/<%= post.author?.id %>"><%= post.author?.username%></a> <%= post.timestamp.toLocaleString('en-US') %>
      </p>
    </p>
    <p><%= post.content %>
    </p>
  </div>

  <div>
    <h2> Similar posts </h2>
    <% for (let i = 1; i < similarPosts.length; i++) { %>
      <div class="post-details">
        <p><a href="/u/<%= similarPosts[i].post.author?.id %>"><%= similarPosts[i].post.author?.username%></a> <%= similarPosts[i].post.timestamp.toLocaleString('en-US') %>
        </p>
        <p><%= similarPosts[i].post.content %>
        </p>
        <p>Similarity: 
          <span style="color: hsl(<%= (1 - similarPosts[i].similarity) * 120 %> 100% 40%)">
          <%= (100 - similarPosts[i].similarity*100).toFixed(0) %>
        </span></p>
      </div>
    <% } %>
  </div>
  <hr>
</body>
</html>
